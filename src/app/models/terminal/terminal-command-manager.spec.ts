import { TerminalCommandManager } from './terminal-command-manager';

describe('TerminalCommandManager', () => {
  it('should create an instance', () => {
    expect(new TerminalCommandManager()).toBeTruthy();
  });
});
